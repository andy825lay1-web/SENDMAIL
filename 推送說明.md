# 🚀 Git 推送說明

## ✅ 已提交的更改

以下更改已成功提交到本地倉庫：

1. **package.json** - 添加 `@supabase/supabase-js` 依賴
2. **package-lock.json** - 自動更新的鎖定文件
3. **Supabase_Bucket檢查報告.md** - Supabase Storage 檢查報告
4. **Supabase_RLS政策檢查報告.md** - RLS 政策檢查報告
5. **Supabase用戶登入檢查報告.md** - 用戶登入狀態報告
6. **環境檢查報告.md** - 開發環境檢查報告
7. **Supabase數據遷移指南.md** - 數據遷移指南

**提交記錄**：
```
f0b256d 更新依賴：添加 @supabase/supabase-js 並添加 Supabase 檢查報告和遷移指南
5839cfa 添加 Supabase 數據遷移腳本和指南
```

---

## ⚠️ 推送失敗（認證問題）

**錯誤訊息**：
```
remote: Permission to andy825lay1-web/SENDMAIL.git denied to sky770825.
fatal: unable to access 'https://github.com/andy825lay1-web/SENDMAIL.git/': The requested URL returned error: 403
```

**原因**：
- 當前 Git 使用 `sky770825` 帳號
- 目標倉庫屬於 `andy825lay1-web` 帳號
- 需要使用 `andy825lay1-web` 帳號的認證

---

## 🔧 解決方法

### 方法 1：使用 Personal Access Token（推薦）

#### 步驟 1：生成 Personal Access Token

1. 前往：https://github.com/settings/tokens
2. 使用 **andy825lay1-web** 帳號登入
3. 點擊 "Generate new token (classic)"
4. 填寫以下資訊：
   - **Note**: `SENDMAIL 專案專用`
   - **Expiration**: 選擇合適的過期時間（建議 90 天或 No expiration）
   - **Select scopes**: 勾選 `repo`（完整倉庫權限）
5. 點擊 "Generate token"
6. **重要**：立即複製生成的 Token（格式：`ghp_xxxxxxxxxxxxx`），只會顯示一次

#### 步驟 2：使用 Token 推送

在終端執行：

```bash
cd "/Users/caijunchang/Desktop/程式專案資料夾/冠軍區/郵件發送系統"
git push -u origin main
```

當提示輸入時：
- **Username**: `andy825lay1-web`
- **Password**: 貼上剛才生成的 Personal Access Token（**不是 GitHub 密碼**）

**注意**：Token 會保存在本地，下次推送時不需要再次輸入。

---

### 方法 2：使用 GitHub CLI（如果已登入）

如果您已經使用 GitHub CLI 登入 `andy825lay1-web` 帳號：

```bash
cd "/Users/caijunchang/Desktop/程式專案資料夾/冠軍區/郵件發送系統"
git config --local credential.helper "!gh auth git-credential"
git push -u origin main
```

---

### 方法 3：使用 SSH（最穩定）

#### 步驟 1：生成 SSH 金鑰（如果還沒有）

```bash
ssh-keygen -t ed25519 -C "andy825lay1@gmail.com" -f ~/.ssh/id_ed25519_andy825lay1
```

#### 步驟 2：添加 SSH 金鑰到 GitHub

1. 複製公鑰：
```bash
cat ~/.ssh/id_ed25519_andy825lay1.pub | pbcopy
```

2. 前往：https://github.com/settings/keys
3. 使用 **andy825lay1-web** 帳號登入
4. 點擊 "New SSH key"
5. 貼上公鑰並保存

#### 步驟 3：配置 SSH 使用特定金鑰

編輯 `~/.ssh/config`：

```
# SENDMAIL 專案使用 andy825lay1-web 帳號
Host github-andy825lay1
  HostName github.com
  User git
  IdentityFile ~/.ssh/id_ed25519_andy825lay1
```

#### 步驟 4：更新遠程倉庫地址

```bash
cd "/Users/caijunchang/Desktop/程式專案資料夾/冠軍區/郵件發送系統"
git remote set-url origin git@github-andy825lay1:andy825lay1-web/SENDMAIL.git
```

#### 步驟 5：推送

```bash
git push -u origin main
```

---

## 📋 推薦方法

**建議使用方法 1（Personal Access Token）**，因為：
- ✅ 不需要切換全局帳號
- ✅ 不影響其他專案的 Git 操作
- ✅ 設置簡單快速
- ✅ Token 保存在本地，只需輸入一次

---

## ✅ 推送成功後

推送成功後，您可以在 GitHub 上看到：
- 所有已提交的更改
- Supabase 相關的檢查報告和指南
- 更新的依賴配置

**GitHub 倉庫**：https://github.com/andy825lay1-web/SENDMAIL

---

## 📝 注意事項

- 本專案已設置本地 Git 配置（`--local`），不會影響全局設置
- 其他專案的 Git 操作不受影響
- 如果使用 Personal Access Token，請妥善保管，不要分享給他人
- Token 權限建議設置為 `repo` 即可，不要給予過多權限

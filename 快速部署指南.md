# 🚀 Render 快速部署指南

## ✅ 已完成的準備工作

- ✅ `render.yaml` 配置文件已創建
- ✅ `package.json` 配置正確
- ✅ `server.js` 已支援 `process.env.PORT`
- ✅ 所有文件已推送到 GitHub

---

## 📋 5 分鐘快速部署步驟

### 1️⃣ 登入 Render（1 分鐘）

1. 瀏覽器應該已自動打開：https://render.com
2. 如果沒有，請訪問：https://render.com
3. 點擊 "Get Started for Free"
4. 使用 GitHub 帳號登入（推薦）

### 2️⃣ 創建 Web Service（2 分鐘）

1. 登入後，點擊右上角的 **"New +"** 按鈕
2. 選擇 **"Web Service"**

### 3️⃣ 連接 GitHub 倉庫（1 分鐘）

1. 在 "Connect a repository" 區段：
   - 搜尋：`andy825lay1-web/SENDMAIL`
   - 選擇該倉庫
   - 點擊 **"Connect"**

### 4️⃣ 配置設置（30 秒）

系統會自動檢測配置（因為有 `render.yaml`），但請確認以下設置：

- **Name**: `sendmail-backend`（可自定義）
- **Region**: 選擇最近的區域（如 Singapore）
- **Branch**: `main`
- **Root Directory**: `/`
- **Runtime**: `Node`
- **Build Command**: `npm install`
- **Start Command**: `npm start`
- **Plan**: 選擇 **Free**（免費方案）

### 5️⃣ 開始部署（1 分鐘）

1. 點擊底部的 **"Create Web Service"**
2. 等待 3-5 分鐘，Render 會自動：
   - 從 GitHub 拉取代碼
   - 執行 `npm install`
   - 啟動服務

---

## 🔑 獲取後端 URL

部署完成後，Render 會提供一個 URL，例如：
```
https://sendmail-backend.onrender.com
```

**重要**：複製這個 URL！

---

## 📝 更新前端 API 地址

### 步驟 1：編輯 config.js

打開 `public/config.js` 文件，找到這一行：

```javascript
: 'https://YOUR-BACKEND-URL.onrender.com'; // 👈 部署後端後，修改此處
```

替換為您的 Render URL（不需要 `https://` 前綴）：

```javascript
: 'https://sendmail-backend.onrender.com'; // 👈 改為您的 Render URL
```

### 步驟 2：提交更改

在終端執行：

```bash
cd "/Users/caijunchang/Desktop/程式專案資料夾/冠軍區/郵件發送系統"
git add public/config.js
git commit -m "更新後端 API 地址"
git push
```

### 步驟 3：等待部署

GitHub Pages 會自動檢測更改並重新部署（約 1-2 分鐘）。

---

## ✅ 測試部署

### 測試後端

訪問您的 Render URL：
```
https://sendmail-backend.onrender.com/api/customers
```

應該看到 JSON 響應（可能是空陣列 `[]`）。

### 測試前端

1. 訪問 GitHub Pages：
   ```
   https://andy825lay1-web.github.io/SENDMAIL/
   ```
2. 打開瀏覽器開發者工具（F12）
3. 查看 Console，應該看到：
   ```
   🌐 GitHub Pages 模式
   📡 API 地址：https://sendmail-backend.onrender.com/api
   ```
4. 嘗試載入客戶列表，檢查是否正常連接

---

## ⚠️ 重要提醒

### Render 免費方案限制

- **休眠**：15 分鐘沒有請求會休眠
- **喚醒時間**：首次請求需要 30-60 秒
- **建議**：如果經常使用，考慮升級到付費方案（$7/月）

---

## 🆘 遇到問題？

### 部署失敗

1. 查看 Render 日誌（服務詳情頁面的 "Logs" 標籤）
2. 檢查 `package.json` 是否正確
3. 確保 `server.js` 使用 `process.env.PORT`

### 前端無法連接

1. 檢查 `public/config.js` 中的 `BACKEND_URL` 是否正確
2. 檢查瀏覽器控制台是否有 CORS 錯誤
3. 確認後端服務正在運行

### CORS 錯誤

- 確保 `server.js` 中的 `cors` 設置包含：
  ```javascript
  origin: [
    'https://andy825lay1-web.github.io',
    'http://localhost:3000'
  ]
  ```

---

## 📚 更多信息

- 詳細部署指南：`DEPLOY_RENDER.md`
- 系統架構說明：`ARCHITECTURE.md`
- GitHub Pages 設置：`SETUP_GITHUB_PAGES.md`

---

**完成以上步驟後，您的郵件發送系統就可以正常運作了！** 🎉
